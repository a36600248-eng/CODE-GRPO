## MBPP Smoke Suite

This folder only keeps configs that match the current algorithm.

Files:

1. `raw_qwen7b_eval_mbpp.yaml`
2. `codegrpo_single_round_zero_pass_soft_reward.yaml`
3. `codegrpo_pseudo_multiround_zero_pass_soft_reward.yaml`

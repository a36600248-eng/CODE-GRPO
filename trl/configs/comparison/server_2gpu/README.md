## Dual-GPU Server Configs

Legacy comparison configs were removed.

This folder currently keeps only the raw eval config:

- `raw_qwen7b_eval_mbpp.yaml`

Current train runs should use the dedicated top-level scripts:

- `run_zero_pass_soft_reward_server_2gpu.sh`
- `run_pseudo_multiround_zero_pass_soft_reward_server_2gpu.sh`

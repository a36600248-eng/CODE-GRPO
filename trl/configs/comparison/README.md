## Comparison Configs

The comparison suite is now reduced to the current minimal chain.

Retained configs:

- `server_2gpu_smoke/raw_qwen7b_eval_mbpp.yaml`
- `server_2gpu_smoke/codegrpo_single_round_zero_pass_soft_reward.yaml`
- `server_2gpu_smoke/codegrpo_pseudo_multiround_zero_pass_soft_reward.yaml`

Old vanilla-vs-legacy-method comparison configs were removed because they no longer match the active training code.
